import { ThreeDotsForLongTextDirective } from './three-dots-for-long-text.directive';

describe('ThreeDotsForLongTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeDotsForLongTextDirective();
    expect(directive).toBeTruthy();
  });
});
