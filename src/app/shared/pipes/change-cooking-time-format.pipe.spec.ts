import { ChangeCookingTimeFormatPipe } from './change-cooking-time-format.pipe';

describe('ChangeCookingTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeCookingTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
